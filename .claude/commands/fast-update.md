---
description: 快速更新分支（先 fetch 再 pull）
---

执行项目根目录下的 `fast-pull.sh` 脚本，快速更新当前分支或指定分支。

如果用户在命令后指定了分支名称（如 `/fast-update develop`），则使用该分支；否则更新当前分支。

执行步骤：
1. 检查 `fast-pull.sh` 脚本是否存在
2. 运行 `./fast-pull.sh [分支名]` 命令
3. 如果执行失败，检查错误信息并给出解决建议
4. 执行成功后，显示更新结果
