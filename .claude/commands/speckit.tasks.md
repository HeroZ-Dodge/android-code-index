---
description: 基于可用的设计文档, 为功能特性生成可执行的、按依赖关系排序的 tasks.md 文件.
handoffs:
  - label: 分析一致性
    agent: /speckit.analyze
    prompt: 运行项目一致性分析
    send: true
  - label: 实施项目
    agent: /speckit.implement
    prompt: 实施项目
    send: true
---

## 用户输入

```text
$ARGUMENTS
```

在继续之前, 你**必须**考虑用户输入(如果不为空).

## 概述

1. **设置**: 从仓库根目录运行 `.specify/scripts/bash/check-prerequisites.sh --json` 并解析 FEATURE_DIR 和 AVAILABLE_DOCS 列表. 所有路径必须是绝对路径. 对于参数值中的单引号如 "I'm Groot", 使用转义语法: 例如 'I'\''m Groot'(或尽可能使用双引号: "I'm Groot").

2. **检查审核状态**: 在生成任务之前, 检查是否已执行审核:
   - 检查 `FEATURE_DIR/checklists/plan_review.md` 是否存在

   **如果审核文档不存在:**
   - 显示警告:
     ```
     ⚠️ 未发现审核报告 (checklists/plan_review.md)

     建议在生成任务前先执行计划审核，以确保：
     - 需求覆盖完整
     - 数据模型一致
     - 边界条件已考虑
     - 实现细节充分
     ```
   - 使用 AskUserQuestion 工具询问用户是否继续, 提供以下两个选项:
     - **跳过审核 (推荐)**: 跳过审核，继续生成任务
     - **先审核**: 先执行审核

   - **重要: 用户回复后, 你必须继续执行:**
     - 如果用户选择 "先审核":
       - 显示: `📋 请先执行 /speckit.review，完成后再运行 /speckit.tasks`
       - **在此停止并结束本次 skill 执行**
     - 如果用户选择 "跳过审核" 或其他回复:
       - 显示: `⏭️ 跳过审核，继续生成任务...`
       - **必须立即继续执行下面的步骤 3 - 不要停止**

   **如果审核文档存在:**
   - 检查审核中是否有未解决的严重问题(🔴 严重问题)
   - 如果存在严重问题且未标记为已解决:
     - 显示警告并列出问题数量
     - 仍然继续执行步骤 3(不需要询问)
   - 如果没有严重问题: 继续执行步骤 3

3. **加载设计文档**: 从 FEATURE_DIR 读取:
   - **必需**: plan.md(技术栈、库、结构), spec.md(带优先级的用户故事)
   - **可选**: data-model.md(实体), contracts/(API 端点), research.md(决策), quickstart.md(测试场景)
   - 注意: 并非所有项目都有所有文档. 基于可用内容生成任务.

4. **检查现有 tasks.md**: 检查 `FEATURE_DIR/tasks.md` 是否已存在:

   **如果 tasks.md 存在(增量模式)**:
   - 读取并解析现有的 tasks.md
   - **识别已完成任务**: 带有 `- [x]` 复选框的任务为已完成, 绝对不能修改
   - **识别待处理任务**: 带有 `- [ ]` 复选框的任务可以修改或重组
   - **保留结构**: 保持包含已完成任务的现有阶段结构和任务 ID
   - 显示摘要:
     ```
     📋 检测到现有任务文档

     - 已完成任务: X 个 (将保留不变)
     - 待处理任务: Y 个 (可修改)
     - 当前阶段数: Z 个
     ```
   - 带增量模式标志继续执行步骤 5

   **如果 tasks.md 不存在(全新模式)**:
   - 带全新生成模式继续执行步骤 5

5. **执行任务生成工作流**:
   - 加载 plan.md 并提取技术栈、库、项目结构
   - 加载 spec.md 并提取带优先级的用户故事(P1、P2、P3 等)
   - 如果存在 data-model.md: 提取实体并映射到用户故事
   - 如果存在 contracts/: 映射端点到用户故事
   - 如果存在 research.md: 提取设置任务的决策

   **增量模式(现有 tasks.md)**:
   - 将现有任务与当前 plan/spec 需求进行比较
   - **绝对保留**: 所有已完成任务(`- [x]`) - 内容、ID、位置、阶段
   - **可修改**: 待处理任务(`- [ ]`) - 更新描述、重新排序或移除过时任务
   - **可添加**: 为新需求添加新任务
   - **ID 重编号**: 所有修改完成后, 保留已完成任务(`- [x]`)的原始 ID, 然后从最高已完成任务 ID 之后按文档顺序重新编号所有待处理任务(`- [ ]`)
   - **新内容结构**: 如果需要新的用户故事或阶段, 作为新章节/阶段添加到末尾
   - 生成差异摘要, 显示: 保留、修改、新增、移除(仅限待处理)的任务

   **全新模式(无现有 tasks.md)**:
   - 按用户故事生成任务(参见下面的任务生成规则)
   - 生成显示用户故事完成顺序的依赖关系图
   - 为每个用户故事创建并行执行示例
   - 验证任务完整性(每个用户故事都有所有必需任务, 可独立测试)

6. **生成/更新 tasks.md**:

   **全新模式**: 使用 `.specify/templates/tasks-template.md` 作为结构, 填充:
   - 来自 plan.md 的正确功能名称
   - 阶段 1: 设置任务(项目初始化)
   - 阶段 2: 基础任务(所有用户故事的阻塞先决条件)
   - 阶段 3+: 每个用户故事一个阶段(按 spec.md 中的优先级顺序)
   - 每个阶段包括: 故事目标、独立测试标准、测试(如果请求)、实现任务
   - 最终阶段: 完善与横切关注点
   - 所有任务必须遵循严格的检查清单格式(参见下面的任务生成规则)
   - 每个任务的清晰文件路径
   - 显示故事完成顺序的依赖关系部分
   - 每个故事的并行执行示例
   - 实现策略部分(MVP 优先, 增量交付)

   **增量模式**: 在保留已完成工作的同时更新现有 tasks.md:
   - **关键**: 已完成任务(`- [x]`)是不可变的 - 不要修改内容、ID 或位置
   - 保持所有包含已完成任务的现有阶段在原始位置
   - 根据新需求更新待处理任务(`- [ ]`)
   - **新任务**: 从最高现有 ID 之后继续分配 ID(例如, 如果 T025 存在, 新任务从 T026 开始)
   - **新阶段/章节**: 添加到文档末尾, 不要在现有阶段之间插入
   - **新用户故事**: 创建新的阶段章节, 附加在现有阶段之后
   - 保持与现有任务格式和命名约定的一致性
   - 仅为新增/修改的任务更新依赖关系图

7. **报告**: 输出生成的 tasks.md 路径和摘要:

   **全新模式**:
   - 总任务数
   - 每个用户故事的任务数
   - 识别的并行机会
   - 每个故事的独立测试标准
   - 建议的 MVP 范围(通常只是用户故事 1)
   - 格式验证: 确认所有任务都遵循检查清单格式(复选框、ID、标签、文件路径)

   **增量模式**:
   - 变更摘要:
     ```
     📊 任务更新摘要

     ✅ 已保留 (不可变): X 个已完成任务
     📝 已修改: Y 个待处理任务
     ➕ 新增: Z 个新任务
     🗑️ 移除: W 个过时任务 (仅限未完成)
     📁 新增章节: N 个新 Phase

     任务 ID 范围: T001 - T0XX
     ```
   - 保留的已完成任务列表(简要)
   - 新增任务列表
   - 新增阶段/章节列表
   - 更新的依赖关系图(如有变化)

任务生成上下文: $ARGUMENTS

tasks.md 应该立即可执行 - 每个任务必须足够具体, 以便 LLM 可以在没有额外上下文的情况下完成它.

## 任务生成规则

**关键**: 任务必须按用户故事组织, 以实现独立的实现和测试.

**测试是可选的**: 仅当在功能规范中明确请求测试或用户请求 TDD 方法时才生成测试任务.

### 检查清单格式(必需)

每个任务必须严格遵循此格式:

```text
- [ ] [TaskID] [P?] [Story?] 带文件路径的描述
```

**格式组件**:

1. **复选框**: 始终以 `- [ ]` 开头(markdown 复选框)
2. **任务 ID**: 按执行顺序的序号(T001、T002、T003...)
3. **[P] 标记**: 仅当任务可并行化时包含(不同文件, 不依赖于未完成的任务)
4. **[Story] 标签**: 仅用户故事阶段任务需要
   - 格式: [US1]、[US2]、[US3] 等(映射到 spec.md 中的用户故事)
   - 设置阶段: 无故事标签
   - 基础阶段: 无故事标签
   - 用户故事阶段: 必须有故事标签
   - 完善阶段: 无故事标签
5. **描述**: 带确切文件路径的清晰操作

**示例**:

- ✅ 正确: `- [ ] T001 根据实施计划创建项目结构`
- ✅ 正确: `- [ ] T005 [P] 在 src/middleware/auth.py 中实现认证中间件`
- ✅ 正确: `- [ ] T012 [P] [US1] 在 src/models/user.py 中创建用户模型`
- ✅ 正确: `- [ ] T014 [US1] 在 src/services/user_service.py 中实现 UserService`
- ❌ 错误: `- [ ] 创建用户模型`(缺少 ID 和故事标签)
- ❌ 错误: `T001 [US1] 创建模型`(缺少复选框)
- ❌ 错误: `- [ ] [US1] 创建用户模型`(缺少任务 ID)
- ❌ 错误: `- [ ] T001 [US1] 创建模型`(缺少文件路径)

### 任务组织

1. **来自用户故事(spec.md)** - 主要组织方式:
   - 每个用户故事(P1、P2、P3...)都有自己的阶段
   - 将所有相关组件映射到它们的故事:
     - 该故事所需的模型
     - 该故事所需的服务
     - 该故事所需的端点
     - 如果请求测试: 该故事特定的测试
   - 标记故事依赖关系(大多数故事应该是独立的)

2. **来自合约**:
   - 将每个合约/端点 → 映射到它服务的用户故事
   - 如果请求测试: 每个合约 → 在该故事阶段的实现之前的合约测试任务 [P]

3. **来自数据模型**:
   - 将每个实体映射到需要它的用户故事
   - 如果实体服务多个故事: 放在最早的故事或设置阶段
   - 关系 → 适当故事阶段的服务层任务

4. **来自设置/基础设施**:
   - 共享基础设施 → 设置阶段(阶段 1)
   - 基础/阻塞任务 → 基础阶段(阶段 2)
   - 故事特定的设置 → 在该故事的阶段内

### 阶段结构

- **阶段 1**: 设置(项目初始化)
- **阶段 2**: 基础(阻塞先决条件 - 必须在用户故事之前完成)
- **阶段 3+**: 按优先级顺序的用户故事(P1、P2、P3...)
  - 每个故事内: 测试(如果请求)→ 模型 → 服务 → 端点 → 集成
  - 每个阶段应该是一个完整的、可独立测试的增量
- **最终阶段**: 完善与横切关注点

### 增量模式规则(关键)

当更新现有 tasks.md 时, 以下规则是**强制性的**:

1. **已完成任务保护(绝对)**:
   - 标记为 `- [x]` 的任务是**不可变的** - 永远不要修改、删除、移动或重新编号
   - 它们的任务 ID、描述、文件路径和阶段位置必须保持完全不变
   - 即使已完成的任务看起来不正确或过时, 也不要更改它

2. **待处理任务(可修改)**:
   - 标记为 `- [ ]` 的任务可以:
     - 修改: 如果需求变更, 更新描述或文件路径
     - 移除: 如果不再需要则删除(过时)
     - 重新排序: 在其阶段内更改位置
   - 待处理任务的 ID 将在所有修改后重新编号(参见"添加新任务"部分的重编号逻辑)

3. **添加新任务**:
   - 新任务可以添加到现有阶段或新阶段
   - **任务 ID 重新编号**: 在所有修改完成后, 按顺序重新编号所有待处理任务(`- [ ]`)
   - **已完成任务(`- [x]`)保留其原始 ID** - 永远不要更改
   - 最终任务列表应包含: 保留原始 ID 的已完成任务 + 在最高已完成任务 ID 之后按顺序重新编号的待处理任务
   - 示例: 如果已完成任务为 T001、T003、T007, 则待处理任务变为 T008、T009、T010...

4. **添加新阶段/章节**:
   - 新阶段必须附加到文档末尾
   - 使用从最后一个阶段继续的顺序阶段编号
   - 示例: 如果阶段 5 是最后一个, 新阶段为阶段 6、阶段 7 等
   - 永远不要在现有阶段之间插入新阶段
   - 新阶段中的任务遵循上述重新编号规则

5. **保留文档结构**:
   - 如果现有章节标题包含已完成任务, 保持其不变
   - 维护现有文档的原始格式和约定
   - 以与现有风格一致的方式添加新章节

**正确的增量更新示例**:

原始 tasks.md:
```text
## Phase 1: Setup
- [x] T001 Create project structure
- [x] T002 Setup dependencies
- [ ] T003 Configure logging

## Phase 2: User Story 1
- [x] T004 [US1] Create User model
- [ ] T005 [US1] Implement UserService
```

增量更新后(新增需求):
```text
## Phase 1: Setup
- [x] T001 Create project structure    ← 保留(已完成, ID 不变)
- [x] T002 Setup dependencies          ← 保留(已完成, ID 不变)
- [ ] T005 Configure logging           ← 重新编号(原 T003, 现 T005)

## Phase 2: User Story 1
- [x] T004 [US1] Create User model     ← 保留(已完成, ID 不变)
- [ ] T006 [US1] Implement UserService ← 重新编号(原 T005, 现 T006)

## Phase 3: User Story 2 (NEW)         ← 新章节添加到末尾
- [ ] T007 [US2] Create Order model    ← 新任务, ID 从 T006 之后继续
- [ ] T008 [US2] Implement OrderService
```

**重新编号逻辑**:
1. 找到最高的已完成任务 ID(示例中: T004)
2. 从 T005 开始, 按文档顺序重新编号所有待处理任务
3. 这样可以在每个执行上下文中保持任务 ID 的顺序性
